import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    marginVertical: 10,
    flexDirection: 'row',
    padding: 0, 
    borderRadius: 50,
    borderWidth: 1,
    borderColor: '#ccc',
    alignItems: 'center'
  },
  searchBar:{
    fontFamily: 'outfit',
    fontSize: 14,
    flex: 1
  },
})